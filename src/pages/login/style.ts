import styled from 'styled-components';

export const Container = styled.div`
  .MuiCard-root {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .MuiCardContent-root {
    padding: 1.5rem;
    width: 300px;
  }
`;